apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: lan-pool
spec:
  blocks:
    - start: "192.168.0.240"      # MUST be outside your router's DHCP range
      stop: "192.168.0.250"

